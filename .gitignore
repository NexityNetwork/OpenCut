node_modules/
.next/
.open-next/
.turbo/
.wrangler/
dist/
.env
.env.local
*.log
.DS_Store

# TypeScript incremental build info
*.tsbuildinfo

# Cloudflare credentials never enter this repo - it is a public fork and the
# global API key is full account access. They live in the scratchpad.
.cfenv
*.cfenv

# Python bytecode caches
__pycache__/
*.py[cod]

# rendered hook frames - 43 x 1MB PNGs, they live in R2 and the vault
content-tools/brand/hooks/
